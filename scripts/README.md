# Scripts

Use this folder for manual development utilities, such as one-off source probes or a local refresh command.

Dashboard-triggered refresh logic should live in `src/biosecurity_dashboard/ingestion/` so scripts and the UI can share it.

