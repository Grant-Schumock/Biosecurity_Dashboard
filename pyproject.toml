[project]
name = "biosecurity-dashboard"
version = "0.1.0"
description = "Local biosecurity policy dashboard for legislation and regulatory data."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.35",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
