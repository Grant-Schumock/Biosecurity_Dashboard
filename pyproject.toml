[project]
name = "biosecurity-dashboard"
version = "0.1.0"
description = "Local biosecurity dashboard for surveillance, legislation, and publication data."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

